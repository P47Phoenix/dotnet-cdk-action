# CLAUDE.md

## Repository Memory

This repository has a corresponding entity in the `mcp__memory` knowledge graph (entity name: `dotnet-cdk-action`, type `github_repo`, linked via an `owns` relation from `Michael Connelly`). Query it via `mcp__memory__search_nodes` or `mcp__memory__open_nodes` for cross-repo context, history, and notes captured outside this file.
